# Exploration Queries

Ad-hoc SQL queries for EDA against cached BigQuery tables.

Add queries here as exploration dictates. Name them descriptively:
- `event_type_distribution.sql`
- `null_profile_instance_events.sql`
- `temporal_coverage.sql`
- etc.

All queries should target YOUR cached tables (`dissertation_lebel.*`),
not the public Google dataset.

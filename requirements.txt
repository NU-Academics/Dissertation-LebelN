# Phase 2 (Data Understanding) dependencies. Established during proposal-phase EDA.
polars>=1.0.0
google-cloud-bigquery>=3.25.0
db-dtypes>=1.3.0
jupytext>=1.16.0
ruff>=0.5.0

# Phase 3 (Data Preparation) and Phase 4 (Modeling) dependencies for Chapter 4.

# Core ML stack
scikit-learn>=1.5.0
xgboost>=2.1.0
lightgbm>=4.4.0
imbalanced-learn>=0.13.0

# Statistical analysis and hypothesis testing
scipy>=1.14.0
statsmodels>=0.14.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# Pandas/NumPy at the ML-library boundary
pandas>=2.2.0
numpy>=1.26.0,<2.3.0

# Testing
pytest>=8.3.0
pytest-cov>=5.0.0

# Hyperparameter optimization (RQ1 tuning)
optuna>=3.6.0

# Phase 7 (Operation and Maintenance) dependencies for RQ5.
# Online learning (Adaptive Random Forest, Hoeffding Adaptive Tree) and the
# ADWIN / Page-Hinkley drift detectors.
# Capped below 0.23: later releases raise the numpy floor above the ceiling this
# project holds for the rest of the ML stack, and an unpinned install resolves a
# numpy upgrade that can break unpickling of the saved ensembles mid-session.
# Lift the cap only together with the numpy pin below.
river>=0.21.0,<0.23.0

# Deferred until they are needed (do not install now):
#   prophet>=1.1.5         # RQ3 time-series forecasting candidate
#   shap>=0.46.0           # Phase 5 (Evaluation) interpretability
#   lime>=0.2.0.1          # Phase 5 (Evaluation) interpretability (optional)
#   tensorflow>=2.17.0     # RQ2 simple NN candidate (optional)
#   torch>=2.4.0           # Optional, deep-learning components